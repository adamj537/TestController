# local_config.py — machine-local overrides (not committed)
# Copy this file to local_config.py and fill in your values.

WIFI_SSID  = "FixtureOpsGateway"
WIFI_PASS  = "gateway123"

TC_IP      = "192.168.50.30"   # TC's IP on FixtureOpsGateway
TC_SERIAL  = "G3-MB-Tester-000"  # TC serial/node ID (mqtt status → serial)
BROKER     = "192.168.50.1"    # MQTT broker (gateway router)
